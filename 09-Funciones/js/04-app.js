const numero1 = 20;
const numero2 = '20';

console.log(parseInt(numero2)); //nombre y parentesis la variable, es una funcion
console.log(numero1.toString()); //seguido de la variable con punto, es un metodo
