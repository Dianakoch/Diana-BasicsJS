const producto = "Monitor 20 pulgadas";
console.log(producto);

console.log(producto.length); //cuantas letras tiene el string


console.log(producto.indexOf('Monitor')); //si encuentra la palabra similar regresa la posicion
console.log(producto.includes('Monitor')); //regresa si es true o false
