//query selector All retorna todos los elementos

const card = document.querySelectorAll('.card');
console.log(card);

const formularios = document.querySelectorAll('#formulario'); //este nos puede retornar todos los elementos que concuerden
console.log(formularios);