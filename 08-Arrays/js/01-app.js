const numeros = [10,20,30]; //objetos con {}, arreglos con [], los arrays soo tienen valores, los objetos tienen llave: valor

const meses = new Array('Enero, Febrero, Marzo');

console.log(numeros); //los arreglos utilizan indices para obtener la posicion en el arreglo (0,1,2,3...)
console.log(meses);


//arreglos con datos de todo tipo
const deTodo = ['Hola', 10, true,'Si', null, {nombre:'Diana', trabajo:'estudiante'}, [1,2,3]]; //una array puede tener diferentes tipos de elementos
console.log(deTodo);